export const homepageQuery = `*[_type == "homepage"][0]`;
export const industriesQuery = `*[_type == "industry"] | order(title asc)`;
export const capabilitiesQuery = `*[_type == "capability"] | order(title asc)`;
